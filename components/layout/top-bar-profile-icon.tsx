import { IconButton } from "@mui/material";

const TopBarProfileIcon = async () => {
  return (
    <div>
      <IconButton size="large" edge="start" aria-label="menu" type="submit"></IconButton>
    </div>
  );
};

export default TopBarProfileIcon;
